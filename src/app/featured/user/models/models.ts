export interface User {
    id: number | null,
    name: string | null,
    surname: string | null,
    email: string | null,
    password: string | null,
    token: string | null,
    role: string | null,    
}
